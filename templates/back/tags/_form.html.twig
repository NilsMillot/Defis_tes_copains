<div class="row">
    {{ form_start(form) }}
    <div class="row">
        <div class="input-field col s6">
            {{ form_widget(form.name) }}
            <label for="challenges_name">Name</label>
        </div>
    </div>
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
</div>
