<form method="post" action="{{ path('admin_signalement_delete', {'id': signalement.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement.id) }}">
    <button style=" outline: none; background: none;"><i class="red-text darken-4 material-icons small">close</i></button>
</form>
