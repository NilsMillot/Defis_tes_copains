<form method="post" action="{{ path('statistical_delete', {'id': statistical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statistical.id) }}">
    <button class="btn">Delete</button>
</form>
