{% extends 'base.html.twig' %}

{% block title %}Statistical{% endblock %}

{% block body %}
    <h1>Statistical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statistical.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ statistical.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('statistical_index') }}">back to list</a>

    <a href="{{ path('statistical_edit', {'id': statistical.id}) }}">edit</a>

    {{ include('statistical/_delete_form.html.twig') }}
{% endblock %}
