{% extends 'base_front.html.twig' %}

{% block title %}New Challenges{% endblock %}

{% block body %}
    <h1>Create new Challenges</h1>

    {{ include('challenges/_form.html.twig',{'button_label': 'Editer'}) }}

    <div style="padding: 30px">
        <a class="btn btn-lg" href="{{ path('challenges_index') }}">Retour à la liste</a>
    </div>
    <style>
        #date{
            font-size: 10px;
            text-align: end;
        }
        a.btn{
            display: block;
            margin: auto;
            width: 180px;
        }
    </style>
{% endblock %}
