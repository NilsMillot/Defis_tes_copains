<form method="post" action="{{ path('challenges_delete', {'id': challenge.id}) }}">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ challenge.id) }}">
	<button style="display: inline-flex; align-items: center; padding: 0 7px; height: 28px;" class="waves-effect waves-light btn red accent-3">Supprimer</button>
</form>
