{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

<style>
    button{
        margin-top: 30px;
        margin-bottom: 30px;
    }
</style>