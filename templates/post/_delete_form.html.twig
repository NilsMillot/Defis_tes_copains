<form method="post" action="{{ path('post_delete', {'id': post.id,'id_challenge':challenge.id}) }}">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
	<button style="display: inline-flex; align-items: center; padding: 0 7px; height: 28px;" class="waves-effect waves-light btn red accent-3 btn_delete_generic">
		<i class="material-icons delete">delete</i>Supprimer</button>
</form>
